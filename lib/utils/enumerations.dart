enum DurationType { weekly, daily, monthly, months }

enum SavingsType { goal, bullion, savingsChallenge, rotarySavings }

enum PaymentPlan { upfront, maturity, monthly }

enum MoreActionType { reminder, user }

enum ProcessorType { card, bank, jara, reserve, alaajo }

enum LendaType { standard, pro, elite }

enum BalanceType {
  total,
  loans,
  jara,
  savings,
  borrows,
  bullion,
  goalSaver,
  savingsChallenge,
  rotary,
}
